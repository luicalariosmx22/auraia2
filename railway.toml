[build]
command = "pip install -r requirements_whatsapp.txt"

[deploy]
startCommand = "python start_railway.py": 2
build:
  commands:
    - apt-get update && apt-get install -y chromium-browser chromium-chromedriver
    - pip install -r requirements_whatsapp.txt
start:
  command: python start_real_whatsapp.py
